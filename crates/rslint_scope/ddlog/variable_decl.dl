import ast
import inputs

typedef VariableDeclKind = VarDeclVar | VarDeclLet | VarDeclConst

/// Variable declarations, an aggregate of `var`, `let` and `const`
relation VariableDecl(
    stmt: StmtId,
    kind: VariableDeclKind,
    pattern: Option<IPattern>,
    value: Option<ExprId>,
    exported: bool,
)

VariableDecl(stmt, VarDeclVar, pattern, value, exported) :-
    VarDecl(stmt, pattern, value, exported).

VariableDecl(stmt, VarDeclLet, pattern, value, exported) :-
    LetDecl(stmt, pattern, value, exported).

VariableDecl(stmt, VarDeclConst, pattern, value, exported) :-
    ConstDecl(stmt, pattern, value, exported).
