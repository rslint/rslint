#[has_side_effects]
extern function dbg(val: 'T)

#[has_side_effects]
extern function debug(val: 'T)

// TODO: Put in ddlog std
function or_else(option: Option<'T>, option_b: Option<'T>): Option<'T> {
    match (option) {
        Some -> option,
        None -> option_b
    }
}
